<p><%= t('views.devise.mailer.reset_password_instructions.hello', email: @resource.email) %></p>

<p><%= t('views.devise.mailer.reset_password_instructions.intro') %></p>

<p><%= link_to t('views.devise.mailer.reset_password_instructions.link'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('views.devise.mailer.reset_password_instructions.ignore_notice') %></p>
<p><%= t('views.devise.mailer.reset_password_instructions.expiry_notice') %></p>
