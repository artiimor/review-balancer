<p><%= t('views.devise.mailer.unlock_instructions.hello', email: @resource.email) %></p>

<p><%= t('views.devise.mailer.unlock_instructions.intro') %></p>

<p><%= t('views.devise.mailer.unlock_instructions.link_intro') %></p>

<p><%= link_to t('views.devise.mailer.unlock_instructions.link'), unlock_url(@resource, unlock_token: @token) %></p>
